# some stdlib modules we use - already installed but listed for information: 
# math, random, itertools, functools, copy, inspect, ast 

# the below modules are used in some extension code which is not currently part of PTO
# numpy
# matplotlib
# scipy
# colorful # https://pypi.org/project/colorful/
# astor # https://pypi.org/project/astor/ # do we actually use it? often commmented-out
# wrapt # https://pypi.org/project/wrapt/
# graphviz # optional, for use in creating visualisations of PTO "derivation trees" !conda install --yes --prefix {sys.prefix} graphviz
# networkx # optional, for use in creating visualisations of PTO "derivation trees"
